import { colors, spacing, fontSizes, fontWeights, radii, shadows } from "./tokens";

export const theme = {
  colors,
  spacing,
  fontSizes,
  fontWeights,
  radii,
  shadows,
  fonts: {
    main: "'Roboto Mono', monospace",
  },
};