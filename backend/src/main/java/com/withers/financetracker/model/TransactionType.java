package com.withers.financetracker.model;

/**
 * Defines whether a transaction represents income or expense.
 * Amount values are always stored as positive numbers.
 */
public enum TransactionType {
    INCOME,
    EXPENSE
}
